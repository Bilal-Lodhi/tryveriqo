{
  "name": "tryveriqo",
  "version": "0.1.0",
  "private": true,
  "description": "tryveriqo — AI-assisted assessment generation with candidate session telemetry and reviewer integrity signals",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Bilal-Lodhi/tryveriqo.git"
  },
  "homepage": "https://github.com/Bilal-Lodhi/tryveriqo#readme",
  "bugs": {
    "url": "https://github.com/Bilal-Lodhi/tryveriqo/issues"
  },
  "type": "module",
  "workspaces": [
    "packages/mcp-mongodb",
    "apps/api"
  ],
  "scripts": {
    "build": "npm run build -w @assessment/mcp-mongodb && npm run build -w @assessment/api",
    "build:api": "npm run build -w @assessment/api",
    "build:mcp": "npm run build -w @assessment/mcp-mongodb",
    "clean": "npm run clean -w @assessment/mcp-mongodb && npm run clean -w @assessment/api",
    "typecheck": "tsc -b --force",
    "test": "npm run test:backend && npm run test:console",
    "test:backend": "npm run build:mcp && npm run test -w @assessment/api && npm run test -w @assessment/mcp-mongodb",
    "test:console": "node scripts/flutter.mjs test",
    "dev": "node scripts/dev-services.mjs",
    "start": "node scripts/dev-services.mjs",
    "lint:console": "node scripts/flutter.mjs analyze",
    "smoke": "node scripts/smoke.mjs",
    "verify": "node scripts/verify-lifecycle.mjs"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
